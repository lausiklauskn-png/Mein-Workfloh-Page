{
  "name": "mein-workfloh-page",
  "version": "1.0.0",
  "private": true,
  "description": "Muster Werbetechnik — zugleich SBKIM-Endknoten",
  "scripts": {
    "test": "node tools/drift-guard.mjs && for f in tests/*.mjs; do case \"$f\" in *gegenprobe*) ;; *) echo \"── $f\"; node \"$f\" || exit 1;; esac; done",
    "gegenprobe": "node tests/sbkim.gegenprobe.mjs",
    "serve": "python3 -m http.server 8000"
  }
}
